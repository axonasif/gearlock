#!/gearlock/bin/bash -i
set +o history
echo -e "
    The basic controls are done with Ctrl, Ctrl+x Key, Alt+Key / FunctionKey, ESC.
    For top menu you need to press Alt + 9 / F9.
    You can also run 'mc' command in virtual terminal.
    
    Press any key to continue ...\c" && read -n1 && mc /data
