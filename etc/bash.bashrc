test -d "/system/ghome" && export HOME="/system/ghome" || export HOME="/data/ghome"; export PATH="/gearlock/bin:/sbin:/bin:/system/bin:/system/xbin:/system/vendor/bin" HISTFILE="$HOME/.bash_history"; PS1="\033[1;34m\u@gearlock\033[0m:\033[1;31m\w$\033[0m "; if [ -n "$RECOVERY" ]; then shopt -q login_shell && geco "You are running GearLock bash under recovery mode.\nYou can switch vt by pressing ALT + F1/2/3.\nRun \033[1;33mnewvt\033[0m command to make another virtual terminal." && newvt(){ openvt -s bash -l; }; trap "chvt 1" EXIT; fi; shopt -s histappend checkwinsize; HISTCONTROL=ignoreboth; HISTSIZE=-1; HISTFILESIZE=-1; if test -d "/system" && test -d "/data" && test -z "$RECOVERY"; then export ANDROID_ROOT="/system" ANDROID_DATA="/data"; else export ANDROID_ROOT="/gearlock" ANDROID_DATA="/gearlock/tmp"; fi
